---
- hosts: localhost
  remote_user: root
  roles:
    - role_under_test
  vars:
    supervisor_programs:
      helloworld:
        command: /bin/cat /etc/os-release
        autostart: "false"
        exitcodes: 0,1,2
